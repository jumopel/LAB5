<Window x:Class="HorseRacingSimulation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:HorseRacingSimulation.ViewModels"
        mc:Ignorable="d"
        Title="Симуляція кінних перегонів" Height="600" Width="900">

    <Window.DataContext>
        <viewmodels:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Margin="0,0,10,10" Padding="10">
            <StackPanel>
                <TextBlock Text="Ставки" FontSize="20" FontWeight="Bold" Foreground="Coral" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="{Binding Balance, StringFormat='Баланс: {0}$'}" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <TextBlock Text="Сума ставки: " VerticalAlignment="Center"/>
                    <TextBox Text="{Binding BetAmount}" Width="50" TextAlignment="Center"/>
                </StackPanel>

                <TextBlock Text="Оберіть коня:" HorizontalAlignment="Center" Margin="0,10,0,5"/>
                <ComboBox ItemsSource="{Binding Horses}" SelectedItem="{Binding SelectedHorseToBet}" DisplayMemberPath="Name" Margin="0,0,0,20"/>

                <Button Content="ЗРОБИТИ СТАВКУ" Command="{Binding PlaceBetCommand}" Width="120" Height="30" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="0" Grid.Column="1" Margin="0,0,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border BorderBrush="Black" BorderThickness="1" Background="LightGreen">
                <Image x:Name="RaceTrackImage" Stretch="Fill"/>
            </Border>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="Почати гонку" Command="{Binding StartSimulationCommand}" Width="120" Height="30" Margin="0,0,10,0"/>
                <TextBlock Text="Кількість коней:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox Width="50" SelectedIndex="0" Margin="0,0,10,0">
                    <ComboBoxItem Content="4"/>
                </ComboBox>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                  ItemsSource="{Binding Horses}" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ім'я" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Позиція (X)" Binding="{Binding PositionX}" Width="100"/>
                <DataGridTextColumn Header="Час" Binding="{Binding RaceTime, StringFormat='hh\\:mm\\:ss\\.fff'}" Width="150"/>
                <DataGridTextColumn Header="Коефіцієнт" Binding="{Binding Coefficient}" Width="100"/>
                <DataGridTextColumn Header="Ставка" Binding="{Binding MoneyBet}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>